services:
  xpfarm:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./data:/app/data
      - ./screenshots:/app/screenshots
      # Mount config/keys if needed?
      # - ./.env:/app/.env 
    environment:
      - GIN_MODE=release
    restart: unless-stopped
